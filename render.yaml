services:
  - type: web
    name: student-management-system
    runtime: python
    buildCommand: |
      pip install -r requirements.txt
      python manage.py collectstatic --noinput
      python manage.py migrate
    startCommand: gunicorn myprojects.wsgi:application
    envVars:
      - key: PYTHON_VERSION
        value: 3.12
